# Alpaca MA5 项目代理准则

本文件适用于本仓库及其所有子目录。任何新的 Codex/代理任务进入本项目后，必须先执行下面的启动流程。

## 每次任务开始前必须执行

1. 在检查代码、运行命令、修改文件、启动任务或连接券商前，完整阅读 `docs/PROJECT_OPERATIONS.md`。
2. 在修改任何源码、脚本、测试、配置或前端文件前，必须再完整阅读根目录 `CODE_MODIFICATION_RULES.md` 并遵守其中的授权边界、验证矩阵和交付格式；不得只读摘要或依赖历史记忆。
   首次进度更新必须简短说明两份准则均已完成阅读，并注明本次任务是否可能触及 Live 账户、下单、监控、WatchCode 或定时任务。
3. 阅读后先运行 `git status --short`，区分用户已有改动、运行时生成文件和本次准备修改的文件；不得覆盖无关改动。
4. 确认当前 `.env` 连接的是 Paper 还是 Live。除非用户明确授权真实下单，否则验证只能读取账户、日历、持仓、订单状态和行情，禁止调用任何提交订单入口。
5. 检查本项目是否已有 WatchCode、监控、网页或定时任务进程；不得重复启动生成器或监控器。
6. 涉及构建、全量测试、数据修复、回测或大规模 WatchCode 生成时，先确认没有其他本机重型任务，再保持全局串行。

如果 `docs/PROJECT_OPERATIONS.md` 或 `CODE_MODIFICATION_RULES.md` 不存在、无法完整读取或彼此冲突，停止会改变状态的操作，先报告并修复文档入口。

## 不可违反的项目约束

- 永远不要重启这台电脑。
- 不得为了测试提交真实订单；`tools/run_test_order.py` 只有在用户明确要求真实测试单时才能运行。
- 停止服务时只能终止命令行明确属于本项目的进程，禁止批量结束全部 `python.exe`、PowerShell、PyCharm 或浏览器进程。
- 美股交易日判断必须复用 `alpaca_ma5_service/trading_calendar.py`，不得重新写“仅排除周末”的判断。
- 以后所有需要日线的回测必须优先只读 `backtest/data/market_data.sqlite`；覆盖不足时先明确报告，不得静默改用其他日线库或把分钟线写入正式日线库。确需补数据时，使用正式重建入口或经用户明确授权的独立缓存。
- 盘中真实买入窗口必须保持为交易日 `09:30 <= t < 12:00 ET`；盘前推荐链路不得提交买单。
- 启动监控前必须确认当日 WatchCode；缺失或过期时先生成，并等待已有盘前/盘中生成任务完成。
- 网页复盘按用户选择的日期展示该日数据；无交易、休市或停牌时可以为空，但不得偷偷回退到前一个有数据日期。
- “净资金流水”不能当作“当日收益”；手动在券商买入造成的本地记录不一致属于正常解释项，不得自动篡改本地账本去强行对齐。
- `.env`、API key、账户信息和通知目标不得写入日志、测试快照、提交内容或回复正文。
- 修改运行逻辑后必须按 `CODE_MODIFICATION_RULES.md` 的验证矩阵和 `docs/PROJECT_OPERATIONS.md` 的验收清单验证，并重新启动受影响的网页/服务。

## 文档维护

只要项目结构、运行入口、交易窗口、定时任务、看板接口、数据结构、环境变量、第三方依赖、WatchCode 语义或验证命令发生变化，必须在同一次修改中同步更新 `CODE_MODIFICATION_RULES.md`、`docs/PROJECT_OPERATIONS.md` 和必要的 README 说明。
